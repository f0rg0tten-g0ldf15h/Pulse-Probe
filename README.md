# Pulse-Probe
API &amp; Microservice Uptime Monitor (Status Page &amp; Health Prober). Users register external URLs/APIs to monitor. The Java backend runs background health checks at intervals, measures latency/response times, tracks error rates, and serves a clean live status dashboard.
